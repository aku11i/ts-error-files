{
  "name": "ts-error-files",
  "version": "0.0.2",
  "description": "",
  "type": "module",
  "module": "dist/main.js",
  "bin": "dist/main.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/main.js",
    "build": "shx rm -rf dist && tsc",
    "dev": "ts-node --esm --transpileOnly src/main.ts",
    "test": "npm run test:e2e",
    "test:e2e": "node --test --loader=ts-node/esm/transpile-only e2e.test.ts",
    "type-check": "tsc --noEmit",
    "prepublishOnly": "npm run test && npm run build"
  },
  "keywords": [],
  "author": "aku11i",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "typescript": "*"
  },
  "dependencies": {
    "ts-morph": "^18.0.0"
  }
}
